# Tomorrow's Work Queue

Date: 2026-05-31

## Rules Engine Features

1. **Rule result streaming** — IAsyncEnumerable for large rule sets (already partially done with ExecuteAsync)
2. **Hot-reload rules** — Update rules without restart
3. **Rule serialization to EF** — Example showing how to store rules in database
4. **Distributed evaluation** — Run rules on multiple nodes
5. **Rule templates** — Pre-defined rule patterns
6. **Rule versioning** — Track rule changes over time
7. **Rule action chaining** — Output of one rule feeds into next
8. **Conditional child rules** — Only evaluate children if parent passes
9. **Rule metrics** — Execution time, success rate tracking
10. **Rule dependency graph** — Visualize rule relationships
11. **Pre-compiled rule library** — Common rules shipped as library
12. **Rule testing framework** — Built-in assertions for rules

## Documentation

13. **Extensive examples** — Real-world use cases: form validation, transaction screening, feature flags, compliance checks
14. **Use case documentation** — When to use Workflow vs RuleBatch vs individual Rule

## Status

- [ ] Not started
